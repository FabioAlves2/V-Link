spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/vmp}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
